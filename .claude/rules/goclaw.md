# GoClaw Integration Rules

- Check official documentation before changing endpoint usage.
- Prefer documented API contracts over assumptions from existing code.
- Map local workspace paths to remote representations explicitly.
- Add regression tests for path flattening, unflattening, memory sync, and archive import/export.
